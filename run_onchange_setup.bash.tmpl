<<<<<<< Updated upstream:run_onchange_setup.bash.tmpl
{{ if eq .chezmoi.os "linux" -}}
#!/usr/bin/env bash

export SCRIPTDIR=$HOME/.local/script
echo $SCRIPTDIR

for i in $SCRIPTDIR/*.sh ; do
    printf "\e[30;42;1m[sh] $i\e[0m\n"
    if [ -r "$i" ]; then
        sh $i
    fi
done

for i in $SCRIPTDIR/*.zsh ; do
    printf "\e[30;42;1m[zsh] $i\e[0m\n"
    if [ -r "$i" ]; then
        zsh $i
    fi
done


{{ end -}}
=======
{{ if eq .chezmoi.os "linux" -}}
#!/usr/bin/env zsh

export SCRIPTDIR=$HOME/.local/script
echo $SCRIPTDIR

for i in $SCRIPTDIR/*.sh ; do
    echo $i
    if [ -r "$i" ]; then
        sh $i
    fi
done

for i in $SCRIPTDIR/*.zsh ; do
    echo $i
    if [ -r "$i" ]; then
        zsh $i
    fi
done


{{ end -}}
>>>>>>> Stashed changes:run_onchange_setup.zsh.tmpl
